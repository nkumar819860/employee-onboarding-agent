<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.employee.onboarding</groupId>
    <artifactId>mcp-servers-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    
    <name>Employee Onboarding MCP Servers Parent</name>
    <description>Parent POM for all Employee Onboarding MCP Server modules</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Mule Runtime and Java versions -->
        <mule.version>4.9.6</mule.version>
        <mule.tools.version>4.2.0</mule.tools.version>
        <mule.maven.plugin.version>4.5.0</mule.maven.plugin.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>

        <!-- MCP Connector version -->
        <mcp.connector.version>1.3.0</mcp.connector.version>

        <!-- Common dependency versions -->
        <jackson.version>2.15.3</jackson.version>
        <commons.pool.version>2.11.1</commons.pool.version>
        <munit.version>3.2.0</munit.version>
        <secure.config.version>1.2.5</secure.config.version>
        <json.module.version>2.5.0</json.module.version>
        <validation.module.version>2.0.4</validation.module.version>
        <http.connector.version>1.11.0</http.connector.version>
        <sockets.connector.version>1.2.7</sockets.connector.version>
        <wsc.connector.version>1.8.6</wsc.connector.version>

        <!-- CloudHub deployment properties -->
        <cloudhub.environment>Sandbox</cloudhub.environment>
        <cloudhub.runtime.version>4.9.6</cloudhub.runtime.version>
        <cloudhub.workers>1</cloudhub.workers>
        <cloudhub.business.group>${project.groupId}</cloudhub.business.group>
        
        <!-- Security and monitoring -->
        <secure.properties>true</secure.properties>
        <anypoint.platform.analytics>true</anypoint.platform.analytics>
    </properties>

    <!-- Child Modules -->
    <modules>
        <module>employee-onboarding-agent-mcp-proxy</module>
        <module>employee-onboarding-system-api</module>
        <module>assets-allocation-system-api</module>
        <module>email-notification-system-api</module>
        <module>employee-onboarding-agent-broker</module>
    </modules>

    <!-- Dependency Management -->
    <dependencyManagement>
        <dependencies>
            <!-- MCP Connector -->
            <dependency>
                <groupId>com.mulesoft.connectors</groupId>
                <artifactId>mule-mcp-connector</artifactId>
                <version>${mcp.connector.version}</version>
                <classifier>mule-plugin</classifier>
            </dependency>

            <!-- Mule HTTP Connector -->
            <dependency>
                <groupId>org.mule.connectors</groupId>
                <artifactId>mule-http-connector</artifactId>
                <version>${http.connector.version}</version>
                <classifier>mule-plugin</classifier>
            </dependency>

            <!-- Mule Sockets Connector -->
            <dependency>
                <groupId>org.mule.connectors</groupId>
                <artifactId>mule-sockets-connector</artifactId>
                <version>${sockets.connector.version}</version>
                <classifier>mule-plugin</classifier>
            </dependency>

            <!-- Mule JSON Module -->
            <dependency>
                <groupId>org.mule.modules</groupId>
                <artifactId>mule-json-module</artifactId>
                <version>${json.module.version}</version>
                <classifier>mule-plugin</classifier>
            </dependency>

            <!-- Mule Validation Module -->
            <dependency>
                <groupId>org.mule.modules</groupId>
                <artifactId>mule-validation-module</artifactId>
                <version>${validation.module.version}</version>
                <classifier>mule-plugin</classifier>
            </dependency>

            <!-- Mule Secure Configuration Property Module -->
            <dependency>
                <groupId>com.mulesoft.modules</groupId>
                <artifactId>mule-secure-configuration-property-module</artifactId>
                <version>${secure.config.version}</version>
                <classifier>mule-plugin</classifier>
            </dependency>

            <!-- WebSocket Connector -->
            <dependency>
                <groupId>org.mule.connectors</groupId>
                <artifactId>mule-wsc-connector</artifactId>
                <version>${wsc.connector.version}</version>
                <classifier>mule-plugin</classifier>
            </dependency>

            <!-- Jackson for JSON processing -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <!-- Apache Commons Pool for connection pooling -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-pool2</artifactId>
                <version>${commons.pool.version}</version>
            </dependency>

            <!-- MUnit Testing Framework -->
            <dependency>
                <groupId>com.mulesoft.munit</groupId>
                <artifactId>munit-runner</artifactId>
                <version>${munit.version}</version>
                <classifier>mule-plugin</classifier>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>com.mulesoft.munit</groupId>
                <artifactId>munit-tools</artifactId>
                <version>${munit.version}</version>
                <classifier>mule-plugin</classifier>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- Build Configuration -->
    <build>
        <pluginManagement>
            <plugins>
                <!-- Mule Maven Plugin -->
                <plugin>
                    <groupId>org.mule.tools.maven</groupId>
                    <artifactId>mule-maven-plugin</artifactId>
                    <version>${mule.maven.plugin.version}</version>
                    <extensions>true</extensions>
                    <configuration>
                        <!-- Alternative deployment configuration using direct username/password -->
                        <!-- encryptProperties REMOVED - use secure properties instead -->
                        <deployment>
                            <uri>https://anypoint.mulesoft.com</uri>
                            <username>${username}</username>
                            <password>${password}</password>
                            <!-- Use encrypted password: mvn encrypt-password yourPassword -->
                        </deployment>
                        
                        <!-- CloudHub 2.0 deployment configuration (alternative) -->
                        <cloudhub2Deployment>
                            <uri>https://anypoint.mulesoft.com</uri>
                            <provider>MC</provider>
                            <environment>${cloudhub.environment}</environment>
                            <muleVersion>${cloudhub.runtime.version}</muleVersion>
                            <username>${anypoint.username}</username>
                            <password>${anypoint.password}</password>
                            <businessGroup>${cloudhub.business.group}</businessGroup>
                            <replicas>${cloudhub.workers}</replicas>
                            <vCores>0.1</vCores>
                            <deploymentSettings>
                                <http>
                                    <inbound>
                                        <lastMileSecurity>true</lastMileSecurity>
                                    </inbound>
                                </http>
                            </deploymentSettings>
                        </cloudhub2Deployment>
                    </configuration>
                </plugin>

                <!-- Maven Compiler Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.11.0</version>
                    <configuration>
                        <source>${maven.compiler.source}</source>
                        <target>${maven.compiler.target}</target>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>

                <!-- Maven Clean Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>3.3.2</version>
                </plugin>

                <!-- Maven Resources Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.3.1</version>
                    <configuration>
                        <encoding>UTF-8</encoding>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <!-- Repositories Configuration -->
    <repositories>
        <repository>
            <id>anypoint-exchange-v3</id>
            <name>Anypoint Exchange</name>
            <url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
            <layout>default</layout>
        </repository>
        <repository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repository</name>
            <url>https://repository.mulesoft.org/releases/</url>
            <layout>default</layout>
        </repository>
        <repository>
            <id>central</id>
            <name>Maven Central Repository</name>
            <url>https://repo1.maven.org/maven2</url>
            <layout>default</layout>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>mulesoft-public</id>
            <name>MuleSoft Public Repository</name>
            <url>https://repository.mulesoft.org/nexus/content/repositories/public/</url>
            <layout>default</layout>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>mcp-connector-repo</id>
            <name>MCP Connector Public Repository</name>
            <url>https://repository.mulesoft.org/nexus/content/repositories/releases/</url>
            <layout>default</layout>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repository</name>
            <layout>default</layout>
            <url>https://repository.mulesoft.org/releases/</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <!-- Environment Profiles -->
    <profiles>
        <!-- Development Profile -->
        <profile>
            <id>dev</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <cloudhub.environment>Development</cloudhub.environment>
                <anypoint.platform.analytics>false</anypoint.platform.analytics>
                <secure.properties>false</secure.properties>
            </properties>
        </profile>

        <!-- Sandbox Profile -->
        <profile>
            <id>sandbox</id>
            <properties>
                <cloudhub.environment>Sandbox</cloudhub.environment>
                <anypoint.platform.analytics>true</anypoint.platform.analytics>
                <secure.properties>true</secure.properties>
            </properties>
        </profile>

        <!-- Production Profile -->
        <profile>
            <id>prod</id>
            <properties>
                <cloudhub.environment>Production</cloudhub.environment>
                <cloudhub.workers>2</cloudhub.workers>
                <anypoint.platform.analytics>true</anypoint.platform.analytics>
                <secure.properties>true</secure.properties>
            </properties>
        </profile>
    </profiles>

</project>
